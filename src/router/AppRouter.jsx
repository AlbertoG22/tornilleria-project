import { Route, Routes } from 'react-router-dom';

export const AppRouter = () => {
  return (
    <Routes>
        {/* Register & Login */}
        <Route />

        {/* Dashboard */}
    </Routes>
  );
};
